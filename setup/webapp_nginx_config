# webapp portion
server {
    # if you have a domain
    # server_name your_domain www.your_domain;

    # else
    # IP address

    location / {
        # specifies some general uWSGI parameters
        include uwsgi_params;

        wsgi_pass unix:/home/swalt/git/cosc310_l09_proj/webapp.sock;
    }


}