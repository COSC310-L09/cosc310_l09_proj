# TODO: Fill the paths properly
# I have this pulled from a personal website I was running, this script will not work at all, but it doesn't need to rn
# I'll leave this for future Sam, fuck that guy
# I'll also add better notes once its properly written, so ya'll can ignore this file

[Unit]
Description=uWGSI instance to serve my home server webpage
After=network.target

[Service]
User=swalt
Group=www-data
WorkingDirectory=/home/swalt/git/cosc310_l09_proj/
Environment="PATH=/home/swalt/git/cosc310_l09_proj/.venv/bin"
ExecStart=/home/swalt/git/cosc310_l09_proj/.venv/bin/uwsgi --ini webapp.ini

[Install]
WantedBy=multi-user.target